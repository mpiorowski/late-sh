//! Zen: two full-bleed pages that cut the clubhouse down to the things you
//! keep alive (the bonsai, the aquarium, the pet) plus the current room and
//! the current track. `7` is the Room, one fixed scene. `8` is Rice, a
//! tiling layout you split, resize, and restyle yourself, persisted per
//! account. Prototype: pure UI, no tests yet.

pub mod bigclock;
pub mod input;
pub mod layout;
pub mod room;
pub mod state;
pub mod ui;
