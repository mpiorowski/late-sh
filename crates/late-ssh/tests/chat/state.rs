// ChatState integration tests that need a full App go here.
// Currently, ChatState fields are pub(crate) and not accessible from
// integration tests. State transition tests that need direct field access
// should be added once public accessors are introduced, or tested
// indirectly through input/render integration tests (e.g. app_input_flow).
